# from django.urls import path
# from api.api import TaskList, TaskDeatil, RegisterList, AirlineList

# app_name = 'flight_scheduler.api'

# urlpatterns = [
#     path('<int:pk>', TaskDeatil.as_view(), name='detailcreate'),
#     path('scheduler/', TaskList.as_view(), name='listcreate'),
#     path('registers/', RegisterList.as_view(), name='listcreate'),
#     path('airlines/', AirlineList.as_view(), name='listcreate'),
# ]
